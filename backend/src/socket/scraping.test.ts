const { getContent, validateSelector, clickElement } = require('./index');

describe('Testing something', () => {
  test('when somthing occurs', async () => {
    expect('dumb').toEqual('dumb');
  });
});
